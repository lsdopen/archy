FROM golang:1.25-alpine
WORKDIR /app

# Install git if needed for go mod download (sometimes needed for private repos, though not here)
# RUN apk add --no-cache git

COPY go.mod go.sum ./
RUN go mod download

COPY . .
RUN go build -o webhook ./cmd/webhook

ENTRYPOINT ["./webhook"]
